
<div class="list-group">
    <% @categories.each do |category| %>
        <%= link_to category, class: 'list-group-item' do %>
		   <%= category.name %> <span class="badge"><%= category.services.size %></span>
		<% end %>
    <%end%>
</div>
